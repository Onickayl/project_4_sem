#ifndef WEATHER_HPP
#define WEATHER_HPP

// объявление глобальных переменных (extern — значит "определены в другом месте")
extern float sun;
extern float temp;
extern float rain;
extern float wind;

#endif